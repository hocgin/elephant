<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="in.hocg.sample.mybatis.example.mapper.TestExampleMapper">

    <select id="findNull" resultType="in.hocg.sample.mybatis.example.entity.TestExample">
        SELECT * FROM test_example te
        WHERE te.id = 10000;
    </select>
    <select id="findNull2" resultType="in.hocg.sample.mybatis.example.entity.TestExample">
        SELECT * FROM test_example te
        WHERE te.id = 10000;
    </select>
</mapper>
