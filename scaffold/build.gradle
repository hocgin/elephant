dependencies {
    compile project(":util")
    compile libs.spring.web
    compile libs.spring.aop
    compile libs.db
    compile libs.quartz
    compile libs.mail
    runtimeOnly libs.spring.devtool
}
